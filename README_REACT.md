# ECOS Mining Game - React Landing Page

React-версия лендинга для ECOS Mining Game, созданная с использованием Vite.

## 🚀 Быстрый старт

### Установка зависимостей
```bash
npm install
```

### Запуск dev-сервера
```bash
npm run dev
```

Приложение откроется автоматически на `http://localhost:3000`

### Сборка для production
```bash
npm run build
```

### Просмотр production сборки
```bash
npm run preview
```

## 📁 Структура проекта

```
Landing Game ECOS/
├── public/              # Статические файлы (изображения)
│   └── images/
├── src/
│   ├── components/      # React компоненты
│   │   ├── Sidebar.jsx
│   │   ├── Home.jsx
│   │   ├── Welcome.jsx
│   │   └── ...
│   ├── styles/          # CSS стили
│   ├── assets/          # Ресурсы
│   ├── App.jsx          # Главный компонент
│   └── main.jsx         # Точка входа
├── css.css              # Основные стили (из оригинального проекта)
├── package.json
├── vite.config.js
└── index.html
```

## 🛠️ Технологии

- **React 18** - UI библиотека
- **Vite** - Сборщик и dev-сервер
- **React Router DOM** - Маршрутизация (если понадобится)

## 📝 Компоненты

### Основные компоненты:
- `App.jsx` - Главный компонент с управлением темой и сайдбаром
- `Sidebar.jsx` - Боковое меню навигации
- `Home.jsx` - Главная страница
- `Welcome.jsx` - Секция Welcome Bonus
- `Assets.jsx` - Секция Assets
- `Tips.jsx` - Секция Tips & Tricks
- `Guides.jsx` - Секция Guides
- `Community.jsx` - Секция Community
- `Visualizer.jsx` - Секция Visualizer
- `Pools.jsx` - Секция BTC Game Pools
- `Ambassador.jsx` - Секция Ambassador Program
- `Leaderboard.jsx` - Секция Leaderboard
- `Events.jsx` - Секция Events
- `City.jsx` - Секция City Screen

## 🎨 Стили

Основные стили находятся в `css.css` (из оригинального проекта).
Все стили применяются глобально через импорт в `main.jsx`.

## 🔧 Настройки

### Vite конфигурация
- Порт: `3000`
- Автоматическое открытие браузера: включено
- Public директория: `public/`

### Темы
Приложение поддерживает светлую и темную темы. Переключение через кнопку в правом верхнем углу.

## 📦 Скрипты

- `npm run dev` - Запуск dev-сервера
- `npm run build` - Сборка для production
- `npm run preview` - Просмотр production сборки
- `npm run lint` - Проверка кода линтером

## 🐛 Отладка

Если возникают проблемы:

1. Убедитесь, что все зависимости установлены: `npm install`
2. Проверьте, что порт 3000 свободен
3. Очистите кэш: `rm -rf node_modules/.vite`
4. Переустановите зависимости: `rm -rf node_modules && npm install`

## 📄 Лицензия

Проект для ECOS Mining Game.

